{
  "name": "webapp-backend",
  "version": "1.0.0",
  "scripts": {
    "start": "json-server --host 0.0.0.0 --watch products.json --port $PORT"
  },
  "dependencies": {
    "json-server": "^0.17.3"
  }
}
